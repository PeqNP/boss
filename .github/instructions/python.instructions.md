---
description: "Use when creating or editing Python private service files."
applyTo: "private/**/*.py"
---

Read the index at the top of `docs/prompt/shared.md` and `docs/prompt/python.md`, then only the sections that apply to this edit. Do not ingest either file whole. Stage 3 / step 4 uses the `private-service-tests` skill.
