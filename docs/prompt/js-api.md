# BOSS JavaScript API Index

**Generated by `bin/boss-api` — do not edit by hand.**

Every public method, grouped by the component that defines it. Use this to
confirm a method exists **on the component you are holding** before calling it.

Several components define the same method name (`selectOption` is on
`UIListBox`, `UIPopupMenu`, `UIMenu`, and `UISearchMenu`), so grepping for a
name only proves it exists *somewhere*. Check the component's own row.

For usage patterns and HTML markup, see [`js.md`](js.md). This file is the
surface only.

---

## `public/boss/ui.js`

### `UIDocument`

`deleteMessage` · `discardMessage`

### `UIControllerConfig`

`borrowerBundleId` · `bundleId` · `name` · `isInteractable` · `isModal` · `isSingleton` · `renderer` · `godot` · `isModule` · `stylesheets` · `sources` · `scrollbar` · `isRemote` · `path` · `modulePath` · `borrow(_bundleId)`

### `Point`

`x` · `y`

### `AppLink`

`bundleId` · `name` · `icon`

### `UIPopupMenuChoice`

`id` · `name` · `data`

### `UIChoice`

`id` · `name` · `child` · `data` · `selected`

### `UIChoiceConfig`

`setModelToData`

### `UITabChoice`

`id` · `name` · `close` · `data`

### `UI`

`POPOVER_ZINDEX` · `VIEWPORT_MARGIN_WIDTH` · `VIEWPORT_MARGIN_HEIGHT` · `desktop()` · `notification()` · `SettingsLocation` · `controller()` · `init()` · `addController(id, ctrl)` · `removeController(id)` · `sendEventsToControllers(events)` · `dragWindow(container)` · `removeWindow(container)` · `focusWindow(container)` · `blurTopWindow()` · `focusTopWindow()` · `appContainerId(bundleId)` · `makePopupMenu(name, label, firstOptionLabel, choices, config)` · `makeListBox(name, choices, config)` · `makeTextField(name, label, config)` · `makeCheckbox(name, label, config)` · `makeWindowAttributes(bundleId, html)` · `makeWindow(bundleId, controllerName, cfg, html, menuId, isSystem)` · `makeModal(bundleId, controllerName, cfg, html, isSystem)` · `registerEmbeddedControllers(container)` · `addOSBarMenu(menu, menuId)` · `addOSBarApp(menu)` · `openSettings(loc)` · `showAboutModal()` · `showTutorial()` · `showInstalledApplications()` · `borrowController(fromApp, toApp, controllerName, uniqueName)` · `borrowBOSSController(controllerName, toApp)` · `showError(error)` · `showDelete(msg, cancel, ok)` · `showConfirmation(msg, cancel, ok)` · `showAlert(msg)` · `showColorPicker(selectColor)` · `showEmbeddedControllers(app)` · `showControllers(app)` · `showEmbeddedController(app, name)` · `showInactivity(secondsRemaining)` · `showRegisterMFA(fn)` · `showInfo(msg)` · `showSignIn()` · `showCreateAccount()` · `showWelcome()` · `showImageViewer(imgs)` · `showProgressBar(title, fn, indeterminate)` · `showBusy()` · `hideBusy()` · `hideAppMenu(bundleId)` · `makeAppButton(config, appMenu)` · `styleUIMenu(menu)` · `styleUIMenus(target)` · `mutex(fn, button, msg)` · `flickerButton(button, msg)` · `toggleDock()` · `showDock()` · `closeDock()` · `hideDock()` · `addAppToDock(app)` · `addAppsToDock(apps)` · `removeAppFromDock(bundleId)` · `updateServerStatus(connected, message)` · `enterKioskMode(title)` · `exitKioskMode()`

### `UIApplication`

`menuId` · `appMenuId` · `scriptId` · `bundleId` · `icon` · `main` · `name` · `passive` · `secure` · `system` · `version` · `scheme` · `defaults` · `proxy()` · `registerController(name, _config)` · `getControllerConfig(name)` · `getEmbeddedControllerNames()` · `getControllerNames()` · `menus()` · `loadController(name, endpoint)` · `applicationDidStart(_main)` · `applyMenuVisibility()` · `applicationWillSignIn(user)` · `applicationWillSignOut()` · `applicationDidStop()` · `applicationDidFocus()` · `applicationDidBlur()` · `sendEvents(events)` · `openDeepLink(deepLink)` · `openUniversalLink(link)`

### `UIWindow`

`id` · `bundleId` · `isModal` · `isInteractable` · `resourcePath` · `init(fn)` · `setDirty(dirty)` · `isDirty` · `showMessage(text, config)` · `hideMessage()` · `setTitle(title)` · `onInit(fn)` · `show(fn)` · `close()` · `didFocusWindow()` · `didBlurWindow()` · `didHitKey(key)` · `didHitEnter()` · `didHitEscape()` · `userDidSignIn(user)` · `userDidSignOut()` · `button(name)` · `div(name)` · `details(name)` · `divByClassName(className)` · `element(id)` · `img(name)` · `p(name)` · `pByClassName(className)` · `fieldset(name)` · `fragment(id)` · `iframe(name)` · `input(name)` · `select(name)` · `pre(name)` · `radio(name, value)` · `span(name)` · `table(name)` · `td(name)` · `textarea(name)` · `menu(name)` · `inputValue(name, msg)`

### `UIController`

`viewDidLoad()` · `viewDidAppear()` · `viewWillUnload()` · `viewDidFocus()` · `viewDidBlur()` · `viewDidZoom(zoom)` · `windowShouldClose()` · `didHitKey()` · `didHitEnter()` · `didHitEscape()` · `userDidSignIn()` · `userDidSignOut()`

### `_UIController`

`button(name)` · `div(name)` · `details(name)` · `divByClassName(className)` · `element(id)` · `img(name)` · `p(name)` · `pByClassName(name)` · `fieldset(name)` · `fragment(fragmentId)` · `iframe(name)` · `input(name)` · `select(name)` · `pre(name)` · `radio(name, value)` · `span(name)` · `table(name)` · `td(name)` · `textarea(name)`

### `UIFolderMetadata`

`name()` · `style()`

### `UIFolder`

`numFolders()`

### `UIPopupMenu`

`selectValue(value)` · `selectOption(index)` · `selectedOption(disabled)` · `selectedValue(disabled)` · `removeAllOptions()` · `addNewOptions(options)` · `enable()` · `disable()` · `styleOptions()`

### `UIMenus`

`select(name)`

### `UIMenu`

`removeOption(value)` · `enableOption(value)` · `disableOption(value)`

### `UIListBox`

**Delegate methods:** `didChangePositionOfListBoxOptions`, `didDeselectListBoxOption`, `didRemoveAllOptions`, `didSelectListBoxOption`

`setDefaultAction(fn)` · `selectValue(value)` · `deselectAllOptions()` · `selectOption(index)` · `removeAllOptions()` · `numSelectedOptions()` · `hasSelectedOption()` · `addNewOptions(options, config)` · `addOption(model)` · `removeOption(value)` · `disableOption(value)` · `enableOption(value)` · `selectedOption()` · `selectedIndex()` · `selectedValue()` · `selectedValues()` · `selectedOptions()` · `moveOptionToPosition(option, newIndex)` · `moveOptionsToPosition(options, newIndex)`

### `UITabs`

`selectTab(value)` · `selectTabIndex(index)` · `addNewTabs(tabs)` · `addTab(model)` · `contains(value)` · `hasTabs()` · `setTabLabel(idx, label)` · `removeTab(value)` · `removeTabIndex(index)` · `getTab(value)` · `selectedTab()` · `selectedValue()`

### `UIPopOverSide`

`left` · `right` · `top` · `bottom`

### `UIPopOver`

`setMessage(msg)` · `show()` · `hide()`

### `UIProgressBar`

`amount` · `setProgress(amt, value)`

### `UISlider`

**Delegate methods:** `didSelectOption`

`selectValue(value)` · `selectOption(index)` · `numSelectedOptions()` · `hasSelectedOption()` · `addNewOptions(options, config)` · `selectedOption()` · `selectedIndex()` · `selectedValue()` · `numOptions()`

### `UISearchMenu`

**Delegate methods:** `didDeselectOption`, `didFocusSearchMenu`, `didSearchForTerm`, `didSelectOption`

`selectOption(choice)` · `clearSelectedValue()`

### `UITokenMenu`

**Delegate methods:** `didAddToken`, `didFocusTokenMenu`, `didRemoveToken`, `didSearchForTerm`

`setTokens(choices)`

---

## `public/boss/os.js`

### `MainController`

`name` · `endpoint` · `configure_fn`

### `DeepLink`

`scheme` · `path` · `parameters`

### `UniversalLink`

`scheme` · `path` · `params`

### `OS`

`online` · `user` · `environment` · `host` · `notificationsOnline` · `email` · `phone` · `isSecurityEnabled` · `network()` · `notification()` · `ui()` · `activeApplication` · `isLoaded()` · `init()` · `isSuperUser(_user)` · `isGuestUser(_user)` · `patchSystemLogger(fn)` · `unpatchSystemLogger()` · `logOut()` · `signInAsGuest()` · `forceLogOut()` · `signIn(_user)` · `refreshSession()` · `pauseMonitoringUserEvents()` · `resumeMonitoringUserEvents()` · `copyToClipboard(button, item)` · `application(bundleId)` · `registerApplications(apps)` · `openApplication(bundleId, mainController)` · `closeApplication(bundleId)` · `switchApplication(bundleId)` · `switchApplicationMenu(bundleId)` · `installedApplications()` · `openDeepLink(deepLink)` · `openUniversalLink(href)` · `getLaunchUrl(bundleId)`

---

## `public/boss/network.js`

### `SessionExpiredError`

`name()`

### `AccessDeniedError`

`name()`

### `NetworkError`

`name()`

### `Network`

`request(url, redirectTo)` · `redirect(url, redirectTo)` · `get(url, decoder)` · `post(url, body)` · `put(url, body)` · `patch(url, body)` · `json(url, body)` · `upload(url, file, body)` · `delete(url, msg, fn, body)` · `stylesheet(href)` · `javascript(href)`

---

## `public/boss/foundation.js`

### `Result`

`ok()` · `error()` · `value()`

---

## `public/boss/notification-manager.js`

### `NotificationError`

`name()`

### `BOSSEvent`

`name` · `userId` · `data`

### `NotificationManager`

**Delegate methods:** `didConnect`, `didDisconnect`, `didReceiveEvents`, `didReceiveNotifications`, `didReceiveResponse`, `didReceiveSessionWillExpireSoon`

`notify(data)` · `start()` · `stop()` · `refreshSession()` · `sendCommand(cmd)` · `send(userId, deepLink, title, body, metadata)` · `sendAppNotification(userId, bundleId, controllerName, deepLink, metadata, userId)` · `seen(notificationIds)` · `delete(notificationIds)` · `sendEvent(name, userId, data)`

---

## `public/boss/application-manager.js`

### `ApplicationManager`

`activeApplication` · `init(apps)` · `registerApplications(apps)` · `installedApplications()` · `application(bundleId)` · `signInAllApplications(user)` · `signOutAllApplications()` · `closeSecureApplications()` · `openApplication(bundleId, mainController)` · `closeApplication(bundleId)` · `switchApplicationMenu(bundleId)` · `switchApplication(bundleId)` · `sendEventsToApplications(events)` · `openDeepLink(deepLink)` · `openUniversalLink(link)`

---

## `public/boss/ui-desktop.js`

### `UIDesktop`

`init()` · `addApp(app)` · `addApps(apps)` · `removeAllApps()` · `removeApp(bundleId)`

---

## `public/boss/ui-notification.js`

### `UINotification`

`init()` · `show(notification)` · `clearAllNotifications()`

